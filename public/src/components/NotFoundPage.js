import React from 'react';
import {BrowserRouter, Route, Switch, Link, NavLink} from 'react-router-dom';
const NotFoundPage = () => (

    <div>
    this is from my not found page - <Link to ="/">Go Home</Link>
    </div>

);

export default NotFoundPage;